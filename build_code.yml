---
- name: Setup code vscode server
  hosts: code
  gather_facts: true
  become: true
  roles:
    # Onboard Server
    - meta_onboard_server

    # Purpose Specific
    - configure_vs_code_remote

    # Reminders
    - maintenance_print_reminders
