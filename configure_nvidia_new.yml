---
- name: Setup nvidia driver on hosts
  # hosts: pvematt,kismnas,stream
  hosts: stream
  gather_facts: true
  become: true
  roles:
    # Preflight

    # Configure Nvidia drivers
    - configure_nvidia_driver_new
