- name: Setup ent jumpbox
  hosts: ent
  gather_facts: true
  become: true
  vars:
    var_one: aaa
    var_two: bbb

  roles:
    # - onboard-packages
    - onboard-user
    - onboard-directories
    - onboard-sshd
    - onboard-timezone
    - onboard-telegraf
    - onboard-firewall
    - onboard-ca-trust
    - setup-host-ent
