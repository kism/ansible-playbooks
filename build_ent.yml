- name: Setup ent jumpbox
  hosts: ent
  gather_facts: true
  become: true
  vars:
    service_account: kism

  roles:
    # Standard onboarding
    - configure-prepare-package-manager
    - configure-server-packages
    - configure-user
    - configure-directories
    - configure-server-sshd
    - configure-timezone
    - configure-telegraf
    - configure-firewall
    - configure-ca-trust

    # Purpose specific
    - configure-fail2ban

    # Host Specific
    - linux-setup-host-ent
