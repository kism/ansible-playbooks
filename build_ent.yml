---
- name: Setup ent jumpbox
  hosts: ent
  gather_facts: true
  become: true
  roles:
    # Preflight
    - meta_include_secrets

    # Onboard Server
    - meta_onboard_server

    # Purpose specific
    - configure_fail2ban
    - configure_jumpbox

    # Reminders
    - maintenance_print_reminders
