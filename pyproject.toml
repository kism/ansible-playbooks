[project]
name = "ansible-playbooks"
version = "0.1.0"
requires-python = ">=3.11, <3.13"
dependencies = [
    "ansible",
    "pywinrm",
    "proxmoxer",
    "httpx",
    "bitwarden-sdk",
    "python-dateutil",
    "requests",
    "ansible-lint",
]

