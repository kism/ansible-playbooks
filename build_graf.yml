---
- name: Setup graf grafana host
  hosts: graf
  gather_facts: true
  become: true
  roles:
    # Onboard Server
    - meta_include_secrets
    - meta_onboard_server

    # Purpose specific
    - configure_grafana

    # Reminders
    - maintenance_print_reminders
