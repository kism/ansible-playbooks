- name: Print ansible facts
  hosts: rproxy
  become: true
  tasks:
    - name: Collect facts
      ansible.builtin.setup:

    - name: Print all available facts
      ansible.builtin.debug:
        var: ansible_facts
