---
- name: Setup stream server
  hosts: stream
  gather_facts: false
  become: true
  roles:
    # Preflight
    # - maintenance_gather_facts_pve
    # - create_opnsense_entries
    # - create_proxmox_ct
    - maintenance_gather_facts

    # Onboard Server
    - meta_onboard
    - configure_docker
    - configure_docker_dispatcharr


    # End
    # - configure_telegraf
    # - configure_elastic_agent
