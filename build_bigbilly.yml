---
- name: Setup bigbilly backup host
  hosts: bigbilly
  gather_facts: false
  become: true
  roles:
    # Preflight
    - maintenance_gather_facts

    # Onboard Server
    - meta_onboard

    # NAS Functinality
    # - configure_disks
    # - configure_mergerfs
    # - configure_cifs_server
    # - configure_rsync_backup_target

    # End
    - configure_telegraf
