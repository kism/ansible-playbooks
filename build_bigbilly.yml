---
- name: Setup cert host
  hosts: bigbilly
  gather_facts: false
  become: true
  roles:
    # Preflight
    - maintenance_gather_facts

    # Onboard Server
    - meta_onboard

    # NAS Functinality
    - configure_disks
    # - configure_cifs_server
