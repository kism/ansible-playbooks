---
- name: Setup code vscode server
  hosts: dpshub
  gather_facts: false
  become: true
  roles:
    # Preflight
    - meta_include_secrets
    - maintenance_gather_facts

    # Onboard Server
    - meta_onboard_workstation

    # End
    - configure_telegraf
    - maintenance_print_reminders
