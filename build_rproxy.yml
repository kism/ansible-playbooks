- name: Setup rproxy, reverse proxy
  hosts: rproxy
  gather_facts: true
  become: true

  roles:
    # General Onboards
    - onboard-packages-server
    - onboard-user
    - onboard-directories
    - onboard-sshd
    - onboard-timezone
    - onboard-telegraf
    - onboard-firewall
    - onboard-ca-trust

    # Host Specific
    - setup-host-rproxy
