- name: Setup Proxmox Host pve
  hosts: pve
  gather_facts: true
  become: true

  roles:
    # Standard server onboarding
    - configure-prepare-package-manager
    - configure-server-packages
    - configure-user
    - configure-directories
    - configure-server-sshd
    - configure-timezone
    - configure-telegraf
    - configure-firewall
    - configure-ca-trust
    - configure-motd

    # Purpose specific
    - configure-concept-baremetal
    - configure-proxmox
    - configure-postfix-gmail

    # # Host Specific
    - linux-setup-host-pve
