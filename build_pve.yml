- name: Setup Proxmox Host pve
  hosts: pve
  gather_facts: true
  become: true

  roles:
    # General Onboards, NO onboard-sshd due to breaking clustering
    - linux-onboard-prepare-package-manager
    - onboard-packages-server
    - onboard-user
    - onboard-directories
    - onboard-timezone
    - onboard-telegraf
    - onboard-firewall
    - onboard-ca-trust

    # Host Specific
    - setup-concept-proxmox
    - setup-host-pve
