---
- name: Unconditionally shut down the machine with all defaults
  hosts: vms
  become: true
  tasks:
    - name: Unconditionally shut down the machine with all defaults
      community.general.shutdown:
