---
- name: Set Timezone To Perth
  hosts: all
  #become: yes
  tasks:
  - name: Set timezone to Perth
    when:
      - timezone == awst
    community.general.timezone:
      name: Australia/Perth

  - name: Set timezone to UTC
    when:
      - timezone == utc
    community.general.timezone:
      name: Etc/UTC

