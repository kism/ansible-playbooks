

server {
    server_name influxdb2.kierangee.au;
    location / {
        allow secret_public_ip1;
        allow secret_local_ip_range1;
        deny all;

        proxy_pass https://ifdb:8086/;
    }
}
