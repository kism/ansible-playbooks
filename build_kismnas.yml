---
- name: Setup kismnas samba host
  hosts: kismnas
  gather_facts: true
  become: true
  roles:
    # Onboard Server
    - meta_include_secrets
    - meta_onboard_server

    # Purpose specific
    - configure_samba
    - configure_jellyfin

    # Reminders
    - maintenance_print_reminders
