---
- name: Setup kismnas samba host
  hosts: kismnas
  gather_facts: true
  become: true
  roles:
    # Preflight
    - meta_include_secrets

    # Onboard Server
    - meta_onboard_server

    # Purpose specific
    - configure_samba
    - configure_jellyfin

    # End
    - configure_telegraf
    - maintenance_print_reminders
