---
- name: Setup x230t tablet
  hosts: x230t
  gather_facts: false
  become: true
  roles:
    # Preflight
    - maintenance_gather_facts

    # Onboard Server
    - meta_onboard

    # End
    - configure_telegraf
