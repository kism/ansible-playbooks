---
- name: Setup cert host
  hosts: cert
  gather_facts: false
  become: true
  roles:
    # Preflight
    - meta_include_secrets
    - maintenance_gather_facts

    # Onboard Server
    - meta_onboard

    # End
    - configure_telegraf
    - maintenance_print_reminders
