- name: Copy nvim settings
  become_user: "{{ service_account }}"
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "~/.config/"
    owner: "{{ service_account }}"
    mode: "0660"
  loop:
    - files/i3
    - files/i3blocks
    - files/rofi
