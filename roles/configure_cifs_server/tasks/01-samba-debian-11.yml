---
- name: Install packages for samba apt {{ ansible_distribution + " " + ansible_distribution_version }}
  ansible.builtin.apt:
    name:
      - samba
      - samba-vfs-modules
      - avahi-daemon
    state: present
  notify:
    - Print smb reminder

- name: Enable services {{ ansible_distribution + " " + ansible_distribution_version }}
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    enabled: true
  loop:
    - smbd
    - avahi-daemon
  notify:
    - Print smb reminder

- name: Allow samba and wsdd through firewalld {{ ansible_distribution + " " + ansible_distribution_version }}
  ansible.posix.firewalld:
    service: "{{ item }}"
    zone: public
    permanent: true
    state: enabled
  notify:
    - Reload firewalld
  loop:
    - samba
    - mdns
