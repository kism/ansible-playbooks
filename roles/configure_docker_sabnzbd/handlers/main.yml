---
- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: reloaded

- name: Reload systemd
  ansible.builtin.systemd:
    name: docker-sabnzbd
    daemon_reload: true
    state: restarted
