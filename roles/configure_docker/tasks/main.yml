---
- name: Configure Docker repo APT
  ansible.builtin.include_tasks: '01-{{ ansible_facts["pkg_mgr"] }}.yml'

- name: Start docker service
  ansible.builtin.systemd_service:
    service: docker
    enabled: true
    state: started
    daemon_reload: true
