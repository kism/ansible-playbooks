---
- name: Non better place, Install fun downloaders
  become: true
  become_user: kism
  community.general.pipx:
    name: "{{ item }}"
    state: install
  loop:
    - internetarchive
    - yt-dlp

- name: Install rsync
  ansible.builtin.package:
    name:
      - rsync
    state: present
