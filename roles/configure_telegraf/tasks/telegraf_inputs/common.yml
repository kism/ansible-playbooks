---
- name: Copy files that are related to baremetal hosts
  ansible.builtin.copy:
    src: files/telegraf.d/15-telegraf_common.conf
    dest: /etc/telegraf/telegraf.d/15-telegraf_common.conf
    mode: "0644"
    owner: root
    group: telegraf
  notify:
    - Restart telegraf
