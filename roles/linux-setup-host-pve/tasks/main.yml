---
- name: Install Wireguard
  ansible.builtin.apt:
    package:
      - wireguard-dkms
    state: latest
  tags:
    - skip-ansible-lint

- name: Add the wireguard module
  community.general.modprobe:
    name: wireguard
    state: present
