---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

  children:
    new_vms:
      hosts:
        KGTEST:
          ansible_host: "{{ new_server_ip_address }}"
          proxmox_vm_id: 199
          proxmox_target_node: pvewill
          proxmox_guest_mac_address: AA:BB:AA:BB:AA:BB
