---
- name: Setup telegraf on all hosts
  hosts: ansible_main
  gather_facts: true
  become: true
  roles:
    # Preflight
    - meta_pre_flight_checks

    # Purpose specific
    - configure_telegraf
