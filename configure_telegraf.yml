---
- name: Setup Proxmox hosts for NFS
  hosts: all
  gather_facts: true
  become: true
  roles:
    # Preflight
    - meta_include_secrets

    # Purpose specific
    - configure_telegraf

    # Reminders
    - maintenance_print_reminders
