---
- name: Setup hs headscale host
  hosts: hs
  gather_facts: false
  become: true
  roles:
    # Preflight
    - maintenance_gather_facts_pve
    - create_opnsense_entries
    - create_proxmox_ct
    - maintenance_gather_facts

    # Onboard Server
    - meta_onboard

    # Purpose specific

    # End
    - configure_telegraf
    - configure_wazuh_agent
